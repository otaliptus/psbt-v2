## go-psbt-v2

This is an attempt to implement psbt-v2 in Go. The end goal is to have a well-documented, functional code that works well for [btcd](https://github.com/btcsuite/btcd/).

Will provide further info later.

```sh
# util commands

# go test -v .
go test -v ./...

golangci-lint run -v --timeout=5m

golangci-lint run --fix
```
